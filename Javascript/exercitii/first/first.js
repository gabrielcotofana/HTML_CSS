var name = "Radu";
var message = "Hello " + name + "!";
var timeout = 3000;
var isNight = true
var nothing = null;

if(isNight) alert("Noapte buna!");

setTimeout(seyHello, timeout);
    function seyHello() {
    // alert(message);

console.log(message); 
}